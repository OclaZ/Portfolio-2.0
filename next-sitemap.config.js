/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://hamzaaslikh.com", // Replace with your domain
  generateRobotsTxt: true, // Generates robots.txt
  sitemapSize: 5000, // Limit the number of entries per sitemap file
  generateIndexSitemap: true, // Ensures a sitemap index is created
};
